import { ExplodeStringToarrayPipe } from './explode-string-toarray.pipe';

describe('ExplodeStringToarrayPipe', () => {
  it('create an instance', () => {
    const pipe = new ExplodeStringToarrayPipe();
    expect(pipe).toBeTruthy();
  });
});
