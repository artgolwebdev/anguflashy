import { ToSlugPipePipe } from './to-slug-pipe.pipe';

describe('ToSlugPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ToSlugPipePipe();
    expect(pipe).toBeTruthy();
  });
});
